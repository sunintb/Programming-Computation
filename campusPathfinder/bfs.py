    #def get_adjacent_list(self):
        #return self.list

    #def drawing_edge(self, r, g, b):

  # def drawing_edges(self, r, g, b):
        #enable_stroke()
        #set_stroke_color(r, g, b)
        #set_stroke_width(5)
        #for adj_vertex in self.adj_list:
            #self.drawing_edge(adj)

# This method returns a string with the selected information (name, pop, longitude, latitude)

